# BookScope - 교육 참고서 추적 대시보드

알라딘 API를 활용한 교육 참고서 모니터링 대시보드입니다. 경쟁사 출판사의 신간 도서와 베스트셀러를 추적하고 검색할 수 있습니다.

## 주요 기능

- 📚 **통합 모드**: 초등/중등/고등 참고서 주간 베스트 10 + 신간 도서
- 🏢 **출판사 필터**: 특정 출판사의 초/중/고 신간 도서 조회
- 🔍 **키워드 검색**: 책 제목, 저자, 출판사로 검색
- 🎨 **인터랙티브 UI**: 마우스 추적 스포트라이트, 3D 카드 효과, 드래그 스크롤
- 🏆 **순위 표시**: 베스트셀러 순위 및 메달 아이콘

## 기술 스택

- **Next.js 16** (App Router)
- **React 19**
- **TypeScript**
- **CSS Modules**

## 시작하기

### 1. 저장소 클론

```bash
git clone https://github.com/your-username/aladin-tracker.git
cd aladin-tracker
```

### 2. 의존성 설치

```bash
npm install
```

### 3. 환경 변수 설정

`.env.local` 파일을 생성하고 알라딘 API 키를 추가하세요:

```bash
ALADIN_API_KEY=your_aladin_api_key_here
```

> **참고**: API 키가 없어도 Mock 데이터로 동작합니다.

### 4. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

## 배포

### Vercel 배포 (권장)

1. [Vercel](https://vercel.com)에 로그인
2. "New Project" 클릭
3. GitHub 저장소 연결
4. 환경 변수 추가:
   - `ALADIN_API_KEY`: 알라딘 API 키
5. "Deploy" 클릭

### 다른 플랫폼

Next.js는 다양한 플랫폼에서 배포할 수 있습니다:
- **Netlify**: [Next.js 배포 가이드](https://docs.netlify.com/integrations/frameworks/next-js/)
- **AWS Amplify**: [Next.js 배포 가이드](https://docs.aws.amazon.com/amplify/latest/userguide/deploy-nextjs-app.html)
- **Railway**: [Railway 배포 가이드](https://docs.railway.app/guides/nextjs)

## 프로젝트 구조

```
aladin-tracker/
├── app/                    # Next.js App Router
│   ├── page.tsx           # 메인 페이지
│   ├── layout.tsx         # 루트 레이아웃
│   └── api/books/         # API 라우트
├── components/            # React 컴포넌트
│   ├── BookCard.tsx       # 책 카드
│   ├── BookRows.tsx       # 책 행 목록
│   ├── SideNav.tsx        # 사이드 네비게이션
│   └── Spotlight.tsx      # 마우스 스포트라이트
├── lib/
│   ├── aladin-client.ts   # 알라딘 API 클라이언트
│   ├── constants.ts       # 상수
│   └── hooks/
│       └── useDraggableScroll.ts
└── public/                # 정적 파일
```

## 환경 변수

| 변수명 | 설명 | 필수 |
|--------|------|------|
| `ALADIN_API_KEY` | 알라딘 Open API 키 | 선택 (없으면 Mock 데이터 사용) |

알라딘 API 키는 [알라딘 Open API](https://www.aladin.co.kr/ttb/api/api_list.aspx)에서 발급받을 수 있습니다.

## 스크립트

- `npm run dev`: 개발 서버 실행
- `npm run build`: 프로덕션 빌드
- `npm run start`: 프로덕션 서버 실행
- `npm run lint`: ESLint 실행

## 보안

**⚠️ 중요**: API 키는 절대 GitHub에 업로드하지 마세요!

- `.env.local` 파일은 `.gitignore`에 의해 자동으로 제외됩니다
- 배포 시에는 배포 플랫폼의 환경 변수 기능을 사용하세요
- 자세한 내용은 `SECURITY.md`를 참고하세요

## 라이선스

이 프로젝트는 내부 사용을 위한 프로젝트입니다.
